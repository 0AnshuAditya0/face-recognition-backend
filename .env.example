# Flask app env config
DEBUG=false
HOST=0.0.0.0
PORT=5000
LOG_LEVEL=INFO
# Comma-separated list or '*' for any origin
CORS_ORIGINS=http://localhost:3000
# Max upload size (in MB)
MAX_CONTENT_LENGTH_MB=20
